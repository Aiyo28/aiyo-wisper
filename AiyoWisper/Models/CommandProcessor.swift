import Foundation

// Phase 3: Voice command processing on selected text
struct CommandProcessor {
    func process(command: String, selectedText: String) async throws -> String {
        selectedText
    }
}
